
// Exporta una función que  devuelva los usuarios activos mayores de 18 años


//Importar los datos desde el archivo data.ts
import { data, User } from "../data";
/*
const anioActual = new Date().getFullYear()
const UsuarioConEdad = users.map(user => {
    const anioCumple = parseInt(user.birthDate.split("-")[0]); //extraer el año
    //split("-") convierte la fecha "YYYY-MM-DD" en un array de 3 partes:
    //"2005-04-10".split("-") 
    // ➡ ["2005", "04", "10"]
    //luego, split("-")[0] toma la primera parte
    //["2005", "04", "10"][0] 
    // ➡ "2005"
    //parseInt(..)
    //Convierte la cadena "2005" en un número entero:
    //parseInt("2005") 
    // ➡ 2005 (número)
    const edad = anioActual-anioCumple;
    return { ...user, edad };
})

//filtrar los mayores de 18 años
const adults=UsuarioConEdad.filter(user => user.edad >= 18);
console.log(adults);
*/
